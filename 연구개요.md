# 터보팬 엔진 잔여 수명 예측: 설계 결정 요인의 교차 데이터셋 절제 연구

> **논문 제목:** From Fleet Normalization to Fault-Mode Gating: A Cross-Dataset Ablation Study of Turbofan Remaining Useful Life Prediction  
> **투고 대상:** Reliability Engineering & System Safety (Elsevier)  
> **최종 수정:** 2026-07-30 (§실험 방법 가설별 귀무/대립가설 및 핵심 논리 추가)

---

## 배경

항공기 터보팬 엔진의 잔여 수명(RUL) 예측은 예지보전(PHM) 시스템의 핵심이다. 늦은 예측(수명 과대추정)은 엔진 고장으로 이어질 수 있고, 이른 예측은 불필요한 정비 비용을 초래한다. NASA CMAPSS 벤치마크(FD001–FD004)는 2008년 이후 이 분야의 표준 데이터셋으로 사용되어 왔다.

그러나 기존 연구들은 RUL 레이블 설계, 센서 정규화, 고장 모드 아키텍처, 손실 함수라는 **4가지 핵심 설계 결정 요인을 한꺼번에 묶어 제안**하기 때문에, 어떤 요인이 성능에 실제로 기여하는지 알 수 없었다.

---

## 목적

4가지 설계 요인을 **독립적으로 통제하며** 각 요인의 성능 기여를 정량적으로 분리한다. 나아가 신뢰성 엔지니어가 PHM 시스템 설계 시 어떤 요인을 **어떤 순서로** 우선해야 하는지에 대한 증거 기반 지침을 제시한다.

---

## 실험 방법

- **데이터:** NASA CMAPSS FD001–FD004 (4개 서브데이터셋, 운전 조건 1개·6개, 고장 모드 1개·2개)
- **공통 백본:** 스택 LSTM (LSTM₁ 64 → LSTM₂ 64 → FC), 슬라이딩 윈도우 30 사이클, 5개 시드 반복
- **통계 검정:** Wilcoxon rank-sum + Benjamini-Hochberg FDR 보정 (α = 0.05), Cohen's d 효과 크기

| 가설 | 변인 | 수준 수 | 기준 모델 |
|------|------|--------|---------|
| H2 | RUL 클리핑 임계값 | 5가지 (75·100·125·130·없음) | Ridge 회귀 |
| H5 | 센서 정규화 방식 | 7가지 (N1–N7) | Fleet MinMax (N1) |
| H6 | 고장 모드 아키텍처 | 4가지 (M0–M3) | 단일 브랜치 (M0) |
| H7 | 훈련 손실 함수 | 7가지 (L1–L7) | MSE (L1) |

---

### H2 — RUL 클리핑 임계값 최적화

**귀무가설 (H₀):** 클리핑 임계값(τ ∈ {75, 100, 125, 130, 미적용})의 선택은 RUL 예측 성능(RMSE, NASA Score)에 통계적으로 유의미한 영향을 미치지 않는다.

**대립가설 (Ha):** 클리핑 임계값에 따라 RMSE·NASA Score가 유의미하게 달라지며, 특정 τ가 다른 값 대비 통계적으로 유의하게 우수하다. 나아가 최적 τ는 데이터셋의 수명 분포에 따라 달라진다.

**핵심 논리 및 근거:**  
터보팬 엔진은 새 상태에서 일정 기간 정상 운전(건강 상태)을 유지한 뒤 열화가 시작된다. 이 초기 정상 구간에 선형 감소 RUL을 그대로 부과하면 모델이 실제 열화가 없는 구간에서도 감소 패턴을 학습하게 된다. 클리핑은 이 초기 구간 RUL을 상수로 평탄화하여 모델이 실제 열화 신호에 집중하도록 유도한다. 학계에서 τ = 125가 경험적 표준으로 자리잡았으나, FD001/FD002 평균 수명 ~206 사이클과 FD003/FD004 평균 수명 ~247 사이클 사이에 약 10%p의 클리핑 비율 차이가 존재해 데이터셋별 최적값이 다를 가능성을 제기한다. 또한 클리핑 임계값과 손실 함수의 상호작용을 교차 분석한 peer-reviewed 논문이 전무해 H7과의 결합 실험이 추가 기여를 창출한다.

**비교 수준:** τ = 75 / 100 / 125(기준) / 130 / 미적용

---

### H5 — 센서 정규화 방식 비교

**귀무가설 (H₀):** 정규화 방식(N1 Fleet MinMax ~ N7 RevIN)의 선택은 RUL 예측 성능에 통계적으로 유의미한 영향을 미치지 않는다. 즉 per-unit 정규화는 fleet 정규화(N1) 대비 통계적으로 유의한 개선을 제공하지 않는다.

**대립가설 (Ha):** 엔진별 초기 관측값을 기준으로 삼는 per-unit 정규화(N3~N6)는 fleet 기준 정규화(N1·N2) 대비 RMSE를 통계적으로 유의하게 낮춘다. 특히 수명이 짧은 엔진(< 150 사이클)에서 개선 폭이 크다.

**핵심 논리 및 근거:**  
Fleet 정규화는 훈련셋 전체 엔진의 평균 센서 범위로 스케일링한다. 엔진마다 새 상태에서의 초기 기준 센서값이 다르기 때문에 이 "초기 조건 분산"이 특징 공간에 그대로 남아 순수한 열화 신호와 혼재한다. Per-unit 정규화는 각 엔진의 초기 관측(초기 5~10 사이클) 기준으로 정규화해 이 초기 조건 차이를 전처리 단계에서 제거하고 모델이 상대적 열화 진행량만 학습하도록 만든다. Consensus MCP 기반 peer-reviewed 논문 200M편 스캔에서 이 비교를 명시적으로 수행한 연구가 0편으로 확인되어 가장 직접적인 Research Gap에 해당한다. FD002/FD004의 경우 6가지 운전 조건으로 인한 센서 절대값 왜곡을 먼저 K-means 잔차화로 제거한 뒤 정규화 비교를 수행해 운전 조건 효과와 정규화 효과를 분리한다.

**비교 수준:** N1 Fleet MinMax(기준) / N2 Fleet Std / N3 Per-unit MinMax(초기 5 사이클) / N4 Per-unit MinMax(10 사이클) / N5 Per-unit Std(5 사이클) / N6 Per-unit Std(10 사이클) / N7 RevIN(학습 가능 인스턴스 정규화)

---

### H6 — 복합 고장 모드 멀티-브랜치 아키텍처

**귀무가설 (H₀):** FD003/FD004에서 고장 모드별 전용 브랜치를 구성해도 단일 LSTM(M0) 대비 RMSE·NASA Score가 통계적으로 유의미하게 감소하지 않는다.

**대립가설 (Ha):** 고장 모드를 분리해 전용 브랜치로 조건부 처리하는 멀티-브랜치 모델(M1~M3)은 M0 대비 FD003/FD004에서 RMSE를 통계적으로 유의하게 감소시킨다. 특히 GMM 사전 클러스터링 없이 초기 10 사이클만으로 라우팅을 학습하는 M3 Attention-Gate가 가장 안정적인 개선을 달성한다.

**핵심 논리 및 근거:**  
FD003/FD004에는 HPC(고압 압축기) 열화와 Fan 열화라는 두 가지 물리적으로 다른 고장 모드가 레이블 없이 혼재한다. EDA에서 FD003 훈련 엔진을 비지도 군집화했을 때 Silhouette 0.634(k=2)의 강한 군집 구조가 확인됐고, 대표 판별 센서 s15(바이패스 비율)의 군집 간 표준화 차이가 |Δz| = 31.3에 달했다. 단일 모델은 두 고장 패턴을 평균하는 방향으로 학습되어 어느 모드도 정확히 예측하지 못한다. H5 실험에서 FD003의 seed 간 RMSE 표준편차가 12.86으로 다른 데이터셋 대비 비정상적으로 높게 나타난 것 자체가 혼재 고장 모드의 진단 신호였다. 기존 Mixture-of-Experts 연구(Expert Syst. Appl. 2025)는 고장 모드 레이블을 사전에 사용하는 지도 방식이며, 비지도 end-to-end 방식의 peer-reviewed 연구는 전무하다.

**비교 수준:** M0 단일 LSTM(기준) / M1 GMM 하드 라우팅 / M2 GMM 소프트 게이팅 / M3 Attention-Gate(초기 10 사이클 기반 end-to-end 라우팅)

---

### H7 — 훈련 손실 함수 최적화

**귀무가설 (H₀):** 비대칭·가중 손실 함수(L2~L7)로 훈련해도 MSE(L1) 대비 NASA Score가 BH-FDR 보정 후 통계적으로 유의미하게 감소하지 않는다.

**대립가설 (Ha):** NASA Score의 비대칭 패널티 구조(지연 예측에 조기 예측보다 더 큰 패널티)를 훈련 손실에 직접 반영하면 MSE 대비 NASA Score가 통계적으로 유의하게 낮아진다. 클리핑 임계값과 손실 함수 사이에는 상호작용 효과가 존재하며 데이터셋별 최적 조합이 다를 수 있다.

**핵심 논리 및 근거:**  
NASA Score는 지연 예측(ŷ > y, 수명 과대추정)에 exp(d/10)−1, 조기 예측(ŷ < y)에 exp(−d/13)−1의 비대칭 패널티를 부과한다. d > 0인 경우 d = 10에서 이미 패널티가 e−1 ≈ 1.72인 반면, d < 0에서 같은 절댓값에 대한 패널티는 e^(10/13)−1 ≈ 1.15에 불과하다. MSE는 잔차 부호에 관계없이 제곱 패널티를 동등하게 부과하므로 MSE로 훈련된 모델은 NASA Score를 직접 최소화하지 않는다. 비대칭 패널티를 손실에 반영하면 모델이 조기 예측 방향으로 편향되어 NASA Score를 낮출 수 있다는 논리다. 그러나 클리핑 임계값이 RUL 레이블 분포 자체를 바꾸므로 손실 함수 효과가 클리핑에 종속될 가능성이 있으며, 클리핑 × 손실 함수 교차 실험을 4개 데이터셋 전체에 걸쳐 수행한 peer-reviewed 연구는 전무하다.

**비교 수준:** L1 MSE(기준) / L2 NASA Score Loss(미분 가능 근사) / L3 DynMSE(수명 단계 동적 가중) / L4 Focal-RUL / L5 TWA(시간 가중 × 비대칭 곱 구조, 본 연구 신규 제안) / L6 Pinball(분위 회귀, τ 탐색) / L7 HubA(Huber × 비대칭, 본 연구 신규 제안)

---

## 핵심 발견

### 🔴 발견 1 — RUL 클리핑 없이는 재앙 (H2) — 필수 / 안전 경계
clip = 125가 전 데이터셋에서 최적 또는 통계적 동점 최적임을 확인.  
**clip = None 적용 시 FD003의 NASA 예지 점수가 13 → 4,014,724으로 306,000배 폭증.**  
단 하나의 전처리 설정 오류가 시스템 전체를 무력화할 수 있음을 정량적으로 입증.

### 🟡 발견 2 — 함대 수준 정규화가 개인 수준 정규화를 압도 (H5) — 중요 / 성능 기반
Fleet MinMax(N1)가 FD001, FD002, FD004에서 모든 개인화(per-unit) 정규화 및 RevIN(N7)보다 통계적으로 유의하게 우수.  
**FD003에서 N1의 seed 간 표준편차가 12.86으로 이상 급등** → 정규화 실패가 아니라, **혼재된 고장 모드가 존재한다는 진단 신호**임을 밝힘.

### 🔴 발견 3 — M3 Attention Gate: 65.8% RMSE 감소 (H6) — 필수 / 핵심 기여
GMM을 완전히 제거하고, 초기 10 사이클만으로 엔진을 고장 모드별 브랜치로 라우팅하는 **M3 (Attention Gate, 4,900 파라미터)** 설계.

| 모델 | FD003 RMSE | FD003 NASA | FD004 RMSE |
|------|-----------|-----------|-----------|
| M0 (기준) | 43.23 ± 0.18 | 34,339 | 28.05 ± 1.74 |
| M1 (GMM 하드 라우팅) | 32.45 ± 11.37 | 27,098 | **49.20 ± 7.17** ← 붕괴 |
| M3 (Attention Gate) | **14.78 ± 1.32** | **425** | 28.33 ± 1.03 |

**M1은 FD004 테스트 시 248개 중 247개 엔진이 동일 브랜치로 배정되는 클러스터 붕괴 발생.**  
M3는 초기 사이클만 사용하므로 이 문제에 면역이며, FD004에서는 기준 모델과 동등한 성능 유지.

### 🟢 발견 4 — 커스텀 손실 함수는 MSE를 이기지 못함 (H7) — 부차적 / 설계 지침
96개 비교(6 손실 × 4 클리핑 × 4 데이터셋) 전체에서 BH-FDR 보정 후 유의한 개선 없음.  
**클리핑 설정이 손실 함수 선택보다 NASA 점수 분산에 압도적으로 지배적.**

---

## 핵심 의미 — 3계층 설계 우선순위

```
TIER 1 [필수] — RUL 레이블 클리핑 (τ = 125)
    오류 시 성능 영향: NASA 점수 최대 30만 배 폭증
    ↓
TIER 2 [중요] — 고장 모드 아키텍처 (FD003류 데이터: M3 적용)
    seed 간 분산을 진단 신호로 활용 → M3로 RMSE 65.8% 개선
    ↓
TIER 3 [부차적] — 손실 함수 선택
    클리핑이 올바를 때 MSE 대비 통계적 이득 없음
```

**실질적 의미:** M3의 GatingNet은 4,900 파라미터(<5% 오버헤드)만 추가하면 되며, **엔진 최초 5 사이클만으로 고장 모드 분류가 가능**하다. 즉 엔진 위탁 시점부터 최적 예측 모델을 즉시 선택할 수 있어, 성능 저하 데이터 축적을 기다릴 필요가 없다.

---

*실험 총 학습 횟수: 800회 이상 | 논문 상태: 투고 준비 완료 | 코드: 수락 후 GitHub 공개 예정*

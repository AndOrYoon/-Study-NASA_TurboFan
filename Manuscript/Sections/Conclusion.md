# VI. Conclusion

> **Draft status:** v1.4 — 2026-08-25 (GPT Rephrasing P3: ≥0.8 threshold softened, "systemic reliability failure"→"most severe performance degradation", "each tier's failure impact"→"each tier produced qualitatively larger effects", Item 50 ordering sentence revised)

---

Four PHM pipeline design factors — RUL label clipping, sensor normalisation, fault-mode architecture, and loss function — were evaluated in isolation across all four NASA CMAPSS sub-datasets (FD001–FD004), with all comparisons Benjamini-Hochberg FDR-corrected. H1 confirmed that clip = 125 cycles — the established standard [5, 6] — produces the lowest or statistically tied-lowest RMSE on all four sub-datasets; removing the RUL ceiling inflates FD003 NASA Score by 306,000-fold, establishing label engineering as a resolved prerequisite throughout the remaining analyses.

Fleet-level min-max normalisation significantly outperformed all per-unit and RevIN strategies on FD001, FD002, and FD004. On FD003, the anomalously high inter-seed variance observed under fleet normalisation proved to be a diagnostic signature of latent fault-mode heterogeneity rather than a normalisation deficiency. Introducing an end-to-end attention-gate architecture (M3) that routes each engine based on its first ten observed cycles resolved this bimodality — the same multi-fault challenge that Ellefsen et al. [7] identified as driving performance differentials on CMAPSS under multiple operating conditions — reducing FD003 RMSE by 65.8% and NASA Score by 98.8% relative to the single-branch baseline. M3 also avoided the test-time cluster-distribution collapse that rendered GMM hard-routing 75.4% worse than baseline on FD004, demonstrating that early-cycle routing is both more accurate and more deployment-robust than post-hoc trajectory clustering. A false-routing sensitivity analysis further examined gate confidence (mean max(w₀, w₁)) as a pre-deployment indicator of routing decisiveness: decisive routing (FD003: 0.840) incurred +48% RMSE and 12-fold NASA Score under forced gate inversion, while diffuse routing (FD004: 0.677) showed negligible impact (+1.6%), with these two observed values suggesting that higher gate confidence is associated with greater sensitivity to routing errors.

No statistically detectable improvement from custom loss functions was found after Benjamini-Hochberg correction across 96 pairwise comparisons (6 losses × 4 clips × 4 datasets). It should be noted that with N=5 seeds and 96 BH-corrected comparisons, the minimum achievable corrected p-value is approximately 3.0 (clipped to 1.0), making statistical significance mathematically impossible regardless of true effect size; the null result should be interpreted as 'no large effect was detectable' rather than confirmed equivalence. The dominant source of variance in all prognostic metrics was RUL clipping rather than loss design. Together, the four hypotheses suggest a three-tier priority ordering — label engineering, then fault-mode architecture (with Silhouette score and gate confidence as sequential diagnostic indicators rather than calibrated thresholds), then loss function — in which each tier produced qualitatively larger effects than the tier below it. Across the present CMAPSS experiments, label specification and fault-mode handling consistently produced larger performance differences than loss-function refinement — a priority ordering that may guide where PHM research and engineering effort is most productively directed.

Collectively, three findings carry direct implications for PHM practice. First, label engineering is a safety-relevant design decision: miscalibrated RUL clipping produced the most severe performance degradation observed across all experiment conditions, dominating all other design choices, and must be resolved before architectural or loss-function work begins. Second, when fault-mode structure is indicated by Silhouette screening, an early-cycle attention gate (M3) achieves accuracy comparable to supervised competitors at negligible computational overhead; gate confidence (mean max(w₀, w₁)) can be examined before deployment as an indicator of routing decisiveness, with the present experiments suggesting that higher confidence is associated with greater sensitivity to routing errors. Third, custom loss functions offer no statistically recoverable benefit once clipping is correctly applied, suggesting that engineering effort is better redirected to the higher-priority tiers. The controlled structure of this ablation also provides an attribution framework for interpreting gains reported by state-of-the-art bundled systems [14, 29, 42]: the evidence indicates that correct label engineering and fault-mode routing account for the majority of systematic performance variation on CMAPSS, while architectural depth and loss-function choice are secondary factors that become distinguishable only after higher-priority tiers are resolved. Taken together, the three-tier design checklist — label engineering, fault-mode architecture, loss function — offers evidence-based design priorities for PHM system configuration, with each tier producing qualitatively larger effects than the tier below it.
